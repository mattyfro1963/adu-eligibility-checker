# Integrity Pass Report

Generated by Integrity Orchestrator after Wave 1 audits + Wave 2 fixes + Wave 3 verify.

## Wave 1 findings (merged)

| # | Severity | Finding |
|---|----------|---------|
| 1 | critical | SB 9 returned `eligible` when `lotSizeSqFt` null; SB9-R5 already `unverified` |
| 2 | high | Green badges with local-confirm language; overlay stub treated as clear |
| 3 | critical | `emptyOverlays()` → UI “Clear” on lot_zoning path |
| 4 | medium | Schematic map vs district PIP — disclaimer understated |
| 5 | medium | “Regulations briefing agent” readable as generative LLM |
| 6 | medium | Statewide floor interleaved with parcel conclusions |
| 7 | high | API `coverage`/`provider` dropped in HomePageClient |
| 8 | high | Universal “Request builder intro” on ResultsCard |

## Wave 2 fixes applied

1. **rules-integrity** — SB 9/ADU require `overlaysVerified === true` for overlay branching; unknown lot area → SB 9 `warning`; unchecked overlays → warning (never eligible claim).
2. **overlay-provenance** — `overlaysVerified` on Parcel/ZoningReport; GIS stub sets `false`; mocks set `true`; UI “Not verified”.
3. **coverage-ux** — Persist `coverage`/`provider` into report; honesty banner beside badge.
4. **geometry-honesty** — MapSiteLegend + approximate-site comments distinguish schematic lot vs district PIP.
5. **regs-framing** — Attribution notes pre-written corpus / not generative AI; lot briefing states state floor vs local confirm.
6. **leadgen-separation** — ResultsCard CTA bifurcates by `overall` (builder / specialist / expert).

## Follow-up (geometry-honesty audit)

After Wave 3, applied remaining high-severity map honesty items from geometry audit:

- Legend: “Schematic lot …” (never “Lot boundary”); always dashed lot swatch; neutral district hatch
- Interactive/static overlays: eligibility colors removed from schematic zoning envelopes
- Lot outline always dashed (area-verified ≠ boundary survey)
- FeaturePin: “lookup point / geocoded location” language
- Eligible widget CTA: “View build-out next steps” (not builder intro)
- SB 9 lot minimum uses shared `SB9_LOT_SPLIT_MIN_SQFT`

## Wave 3 verify (2026-08-24 site-audit pass)

- `npm test`: **137 passed** (integrity regressions retained: unknown lot → SB 9 warning; overlays unverified).
- Acceptance checklist: all seven honesty items remain true.
- Additional major site faults fixed in the same pass (see `SiteAuditReport.md`): Privacy/Terms, embed chrome, demo disclaimer removal, neutral map pin, CTA alignment, sitemap/robots/OG.

## Integrity gate

**Closed** — eligibility honesty criteria met; Vitest green.
