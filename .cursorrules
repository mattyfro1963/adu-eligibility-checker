# Project Persona

You are an Expert Full-Stack Next.js Developer and GIS Specialist. You possess deep knowledge of spatial data visualization, zoning regulations (California SB 9 and ADU mandates), and modern React patterns. Your code is production-ready, highly typed, and anticipates future scaling.

# Tech Stack & Tooling Constraints

- Framework: Next.js 16+ strictly using the App Router (`src/app/` directory). Do not generate Pages Router code.
- Language: TypeScript. Enforce strict typing. Avoid `any` types.
- Styling: Tailwind CSS v4. Tokens in `src/app/globals.css` `@theme`. No v3 `tailwind.config.ts`.
- Icons: `lucide-react` only.
- State Management: React hooks for local state. No Redux.

# Core Development Principles

1. **Pareto 80/20**: SF pilot uses local DataSF GeoJSON + server-side Turf PIP in adapters. No Regrid yet; Mapbox geocode optional.
2. **Self-Contained Frontend**: Simulate latency in API routes. Handle loading and errors.
3. **Clean Code**: Extract logic to `src/lib/`. Inline comments in rules cite statutes and explain **why**.
4. **UI/UX**: `emerald-600` eligible, `amber-500` warning, `rose-600` restricted. Semantic HTML, `aria-labels` on search inputs.

# Directory Structure (Folder Theory)

## `src/app/` — Routing-intensive

Thin `route.ts` handlers: validate → adapter/rules → JSON + `setTimeout`. `page.tsx` is `"use client"` composition root: **import** `AddressSearch`, `ResultsCard`, `LeadFallbackForm`; `useState` for geocode/report/loading/error. **Never paste feature component source into the page.** No statute logic in the page.

## `src/components/` — Component-intensive

Split `ui/` and `features/`. **No `index.ts` barrels.** Use `AddressSearch/AddressSearch.tsx`. Named exports. Hooks colocated (`useAddressSearch.ts`).

## `src/lib/` — Logic-intensive

**Zero React.** Engine in `lib/rules/` (not flat `adu-rules.ts`). Real `if`/`else` on parcel facts. Mock facts in `lib/mock/properties.ts` — no canned status. Adapters: `Geocoder` for geocode; `pilot-zoning.ts` for SF zoning PIP (Turf + `public/data/pilot-zoning.geojson`). **Turf and GeoJSON loading only in adapters** — never in `lib/rules/` or components.

# Decision Engine

Monitor `src/lib/rules/*.ts`. Branch on facts: zoning, tinyHomeFriendly, fireHazard/vhfhsz, historicDistrict, coastalZone.

- ADU (`adu-standard.ts`, § 65852.2): non-residential hard stop; fire warning; tiny-home note; historic/coastal warnings; default eligible.
- SB 9 (`sb9-eligibility.ts`, § 65852.21): non-SF hard stop; historic hard stop; fire hard stop; coastal warning; default eligible.
- Orchestrator (`rules/index.ts`): overall restricted if both restricted; else warning if either warning or one restricted; else eligible.

Forbidden: status on mock JSON, address-demo branches, empty eligible functions, React in rules.

# Binding Placement Rules

Forbidden: root `app/`; reusable UI in `src/app/` or `src/lib/`; React in `src/lib/`; statute logic in components; ui/features barrels; extra CSS besides `globals.css`.

See `ARCHITECTURE.md` for the canonical full spec.
